AVANT RELEASE:
    ajout:
        mettre a jour la version de flib
    graphics:
        
    parameter:
            
    msg erreur:
    
    documentation:
        
    local:
    
        
        




PAS URGENT:
     ? arrivée du pod (reversement image + declaration id )+replique sur toutes les planetes qui l'ont pas ( tout faire dans le final-update)
    choix du bon provider (distance)
    
    delete channel ?
    sound of entity
    sound of gui (ouvert/fermer)